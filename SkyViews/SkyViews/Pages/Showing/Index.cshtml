@page
@model SkyViews.Pages.Showing.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Showing[0].RoomID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Showing[0].FilmID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Showing[0].ShowingTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Showing) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilmID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShowingTime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ShowingID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ShowingID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ShowingID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
